git config --global user.name "name"
git config --global user.email "email"


branching


git checkout commit_id
git checkot 8ada273


git status 
git add .
git commit -m "message"
git push origin master
git log 

git checkout master

we can go to the history in the git commit and can make changes in that to the older versions of the code 
